img = imread('./images/baboon.tiff');
imshow(img, [0,512]);
%1=red, 2=green, 3=blue
red = img(:, :, 1);
green = img(:, :, 2);
blue = img(:, :,3);

img = cat(3, blue, green, red);
figure, imshow(img, [0,512]);

img